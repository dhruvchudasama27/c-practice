// write a programe to check if the given month is fabruary or not if february dispay message ' this month has 28-29 days'
#include<stdio.h>
void main()
{
    int month = 0;

    printf(" the month is");
    scanf("%d",&month);

    // check
    if(month != 2)
    {
         printf(" good byee...");
    }

    printf("\nthis month has 28-29 days" );

}